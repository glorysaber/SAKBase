name: Swift

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v -color-diagnostics
    - name: Run tests
      run: swift test -v
